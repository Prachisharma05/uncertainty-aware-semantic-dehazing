experiment:
  name: b7_scaled_gated_attention
  seed: 42

data:
  train_pairs: data/RESIDE/ITS/train_pairs.txt
  val_pairs: data/RESIDE/ITS/val_pairs.txt
  image_size: 256
  batch_size: 2
  num_workers: 2

model:
  name: unet_scaled_gated_cross_attention
  in_channels: 3
  out_channels: 3
  alpha: 0.1

clip:
  model_name: ViT-B-16
  pretrained: openai
  input_size: 224

training:
  epochs: 3
  learning_rate: 0.000001
  weight_decay: 0.00001
  optimizer: adamw
  mixed_precision: false

loss:
  lambda_l1: 1.0
  lambda_lpips: 0.1
  lambda_clip: 0.03
  lambda_gate: 0.01

saving:
  checkpoint_dir: checkpoints/b7
  result_dir: results/b7
  log_dir: logs/b7